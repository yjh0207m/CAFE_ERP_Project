<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.example.demo.mapper.ProfileMapper">
     <select id="findById" resultType="User" parameterType="String">
        SELECT user_id, user_pw, user_name FROM users WHERE user_id = #{user_id}
     </select>
     <insert id="save" parameterType="User" >
        INSERT INTO users(user_id, user_pw, user_name) VALUES(#{user_id}, #{user_pw}, #{user_name}) 
     </insert>
  </mapper>